// Client-pending: full county list for the 9 homes is not yet confirmed.
// Offaly is the only county currently verifiable from existing data (HQ
// address in src/data/navigation.ts, and the site's own "Co. Offaly and the
// Midlands" copy). Replace with the real per-home county list once the
// client confirms it — this is the single edit point.
export const HIGHLIGHTED_COUNTIES: string[] = ["Offaly"];
